# Configuration for /etc/init.d/haproxy

# Path to the haproxy executable, use this to override the default setting "/usr/bin/octoprint"
HAPROXY=/usr/local/sbin/haproxy

# Log for the configtest run
CONFIGTEST_LOG=/var/log/haproxy_configtest.log

# Additional arguments to pass to haproxy
EXTRA_ARGS=

# Should we run at startup?
ENABLED=1
